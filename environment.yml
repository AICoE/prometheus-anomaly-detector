---
name: prophet-env
channels:
  - conda-forge
dependencies:
  - python=3.6
  - pip
  - fbprophet
  - pip:
      - tensorflow==1.14
      - keras==2.2.5
      - pandas
      - schedule
      - prometheus_client
      - prometheus_api_client
      - tornado
      - dateparser
      - mlflow
      - numpy
      - scikit-learn
      - plotly
