---
name: prophet-env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip
  - fbprophet
  - pip:
      - pandas
      - schedule
      - prometheus_client
      - prometheus_api_client
      - tornado
      - dateparser
      - mlflow
      - numpy
      - keras
      - scikit-learn
